- name: varaibles files
  hosts: local
  connection: local
  vars_files:
    - course.yaml
  tasks:
  - name: print the course info
    ansible.builtin.debug:
      msg: "course name is: {{course}}, DURATION: {{duration}}, trainer: {{trainer}}"