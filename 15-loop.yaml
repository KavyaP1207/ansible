- name: loops
  hosts: local
  connection: local
  tasks:
  - name: print the name in loops
    ansible.builtin.package:
      name: "{{ item }}"
      state: latest
    loop:
    - nginx
    - mysql
    - zip